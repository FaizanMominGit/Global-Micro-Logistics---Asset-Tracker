# OmniTrack Code Map

This document serves as our "Single Source of Truth" API registry and architectural map. We will maintain all critical variables, global functions, and state hooks here so we can instantly reference them without digging through the codebase.

## 1. Global State & Context (Zustand)
<!-- Add active global state variables and slices here as they are built -->

## 2. Core Reusable Hooks (`src/hooks`)
<!-- Add shared hooks here (e.g. useWindowSize, useAuth) -->

## 3. Data Fetching API (`src/features/*/api`)
<!-- Add TanStack Query hooks and API functions here (e.g. useAssetsQuery) -->

## 4. Key Types & Interfaces (`src/types`)
<!-- Add core TS Interfaces here (e.g. Asset, UserContext) -->

## 5. Global Utility Functions (`src/lib` / `src/utils`)
<!-- Add vital utility formatting and calculation algorithms here -->
